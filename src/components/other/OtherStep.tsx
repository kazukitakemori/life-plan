import { HeaderTabPlaceholder } from '../layout/HeaderTabPlaceholder';

export function OtherStep() {
  return (
    <HeaderTabPlaceholder
      title="その他"
      description="税・社会保険料の計算内訳は、画面上部の「資産形成」→「キャッシュフロー表」で「税・社保」の金額をクリックして確認できます。"
    />
  );
}
