name: Apply second-life timing and housing category patch

on:
  push:
    branches:
      - feat/second-life-housing-living-q12
    paths:
      - scripts/tmp-second-life-age-housing-category.py
      - .github/workflows/tmp-second-life-age-housing-category.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/second-life-housing-living-q12
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: npm
      - name: Apply patch
        run: python scripts/tmp-second-life-age-housing-category.py
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Commit verified source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          git commit -m "Separate second-life start age from housing timing" || exit 0
          git push
